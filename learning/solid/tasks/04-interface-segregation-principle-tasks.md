# Tasks — Interface Segregation Principle (ISP)

Easy
- Split a fat interface into two smaller interfaces and update clients.

Medium
- Apply ISP to the `NotificationService` by segregating email/sms/webhook interfaces.

Hard
- Refactor a shared repository interface into read/write-specific interfaces and migrate usage.

Hints
- Keep consumer-specific interfaces minimal.