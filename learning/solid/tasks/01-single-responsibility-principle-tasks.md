# Tasks — Single Responsibility Principle (SRP)

Easy
- Identify and split a function in `backend/src/modules/tasks/task.service.ts` that violates SRP into two smaller functions; add tests.

Medium
- Refactor a controller that mixes validation and business logic into controller + service layers.

Hard
- Create a small module with pluggable strategies (e.g., notification senders) that follow SRP and add tests.

Hints
- Look at `learning/solid/01-single-responsibility-principle.md` for examples.