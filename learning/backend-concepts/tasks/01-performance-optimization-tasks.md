# Tasks — Performance Optimization

Easy
- Profile one endpoint with `clinic` or Node's `--prof` to find CPU hot spots.

Medium
- Fix an N+1 query by adding a join or batching data access.

Hard
- Implement a production-like benchmark harness and optimize memory usage under load.

Hints
- Use `autocannon` for load tests and heap snapshots for memory analysis.