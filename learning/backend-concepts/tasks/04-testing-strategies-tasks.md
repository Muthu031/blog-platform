# Tasks — Testing Strategies

Easy
- Add unit tests for one service using `vitest` and mocks.

Medium
- Add an integration test that runs against a test database and asserts end-to-end behavior.

Hard
- Add an E2E test using Puppeteer (or Playwright) that covers a key user flow.

Hints
- Use test containers or local Docker DB for integration tests.