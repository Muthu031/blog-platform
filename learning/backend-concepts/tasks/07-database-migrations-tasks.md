# Tasks — Database Migrations

Easy
- Add a safe column addition migration and verify app works with old/new code.

Medium
- Implement a data backfill script that runs in batches and is idempotent.

Hard
- Design a zero-downtime column rename strategy with dual-read/dual-write and a rollback plan.

Hints
- Use `prisma` migrations and add a migration test.

