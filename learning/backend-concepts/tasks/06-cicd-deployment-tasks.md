# Tasks — CI/CD & Deployment

Easy
- Add a GitHub Actions workflow to run tests on PRs.

Medium
- Add Docker multi-stage build and push image to local registry or Docker Hub.

Hard
- Implement a blue-green deployment simulation using Kubernetes manifests and switch traffic.

Hints
- Reuse existing `docker-compose.yml` and `backend/Dockerfile` patterns.