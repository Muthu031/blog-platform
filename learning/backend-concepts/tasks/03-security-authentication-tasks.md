# Tasks — Security & Authentication

Easy
- Harden password storage: verify bcrypt usage and add tests for hashing.

Medium
- Implement role-based access checks on a few endpoints and write integration tests.

Hard
- Add OAuth2 login (e.g., Google) and token exchange flow; document security considerations.

Hints
- Check `backend/src/modules/auth` for starter points.