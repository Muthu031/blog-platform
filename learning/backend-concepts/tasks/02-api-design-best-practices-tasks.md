# Tasks — API Design Best Practices

Easy
- Add pagination to the `GET /tasks` endpoint with offset parameters.

Medium
- Implement cursor-based pagination and make clients consume it.

Hard
- Design and implement API versioning strategy and add a v2 endpoint with backward-compatible changes.

Hints
- Update OpenAPI docs and add tests for backward compatibility.