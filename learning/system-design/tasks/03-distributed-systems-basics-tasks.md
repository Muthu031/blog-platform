# Tasks — Distributed Systems Basics

Easy
- Implement retry with exponential backoff for an HTTP call wrapper.

Medium
- Add a circuit breaker around an unstable external service with tests.

Hard
- Implement a simple Saga for multi-step operations across two services (simulate in-process).

Hints
- Use `p-retry` and simple in-memory circuit breaker patterns for prototypes.