# Tasks — Database Optimization

Easy
- Add an index to speed up a slow query and show `EXPLAIN ANALYZE` before/after.

Medium
- Implement cursor-based pagination for a large tasks list endpoint.

Hard
- Partition a table by `orgId` or date and measure query improvements.

Hints
- Use `prisma` raw queries or psql to run `EXPLAIN` locally.