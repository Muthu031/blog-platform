# Tasks — Caching Strategies

Easy
- Implement cache-aside for a single endpoint (see existing cache exercise) and add TTL.

Medium
- Implement cache warming for a hot dataset on startup.

Hard
- Implement write-through or write-behind and analyze consistency trade-offs.

Hints
- Use Redis and the `CacheManager` pattern from the cache exercise.