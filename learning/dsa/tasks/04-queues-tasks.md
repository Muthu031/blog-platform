# Tasks — Queues

Easy
- Implement a FIFO queue with enqueue/dequeue and tests.

Medium
- Implement a circular queue that supports fixed capacity.

Hard
- Build a priority queue (min/max) and use it to implement Dijkstra's algorithm variant.

Hints
- For production-like queues, consider `bull` patterns in `backend`.