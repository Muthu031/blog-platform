# Tasks — Stacks

Easy
- Implement a stack with push/pop/peek and tests.

Medium
- Use a stack to evaluate Reverse Polish Notation expressions.

Hard
- Implement min-stack that returns current min in O(1); add concurrency tests.

Hints
- Use arrays for stack storage; see backend modules under `shared` for patterns.