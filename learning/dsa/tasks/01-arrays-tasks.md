# Tasks — Arrays

Easy
- Implement `unique()` that removes duplicates from an array. Write unit tests.

Medium
- Implement `rotateArray(arr, k)` in-place with O(1) extra space. Add benchmarks.

Hard
- Implement `mergeKSortedArrays(arrays)` using a min-heap; analyze time and memory.

Hints
- Use array methods and Index math for rotate. For hard, see `learning/dsa/07-heaps.md` for heap use.
Starter files: backend/src/shared/utils/array-utils.ts, tests/dsa/arrays.spec.ts