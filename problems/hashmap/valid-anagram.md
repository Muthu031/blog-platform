# Valid Anagram (HashMap)

Difficulty: Easy

Description:
Same as the Strings `Valid Anagram` problem; a good HashMap exercise to count characters.

Input: `s = "anagram", t = "nagaram"`

Output: `true`

Hints:
- Use a hash map to count frequency differences between `s` and `t`.
- Early exit if lengths differ.

Tags: HashMap, Strings
